# Release — Solo Suite Enchance (website)

## Preflight results

Not yet run (`/release:preflight` — this file's normal owner — hasn't been
invoked). The rollback plan below was written directly as part of closing out
`.solo/tasks.md` T6 (P0), ahead of a full preflight pass.

## Deploy mechanism (as understood today — confirm before relying on this)

`README.md:225-230` states the site "deploys through the Sites project
configured in `.openai/hosting.json`" (`project_id: appgprj_6a6776eb920881918c093b1e28e04127`).
**No `deploy` script exists in `package.json`** (only `dev`/`build`/`start`) —
so the actual trigger that ships a build to production (automatic on push?
manual via a dashboard? a separate CLI step not captured in this repo?) is
**not fully established from the repository alone**. What *is* confirmed: the
build output is a standard Cloudflare Worker (`dist/server/wrangler.json` is
generated by `vinext build`), so standard Wrangler deployment tooling applies
to the artifact itself regardless of how the "Sites" layer triggers it.

**Open question for whoever owns the Sites project**: confirm the actual
deploy trigger and update this section once known.

### Strong lead found 2026-08-07 — a second git remote (still UNCONFIRMED)

`git remote -v` shows two remotes, and only `origin` had ever been examined:

```text
origin  https://github.com/Unn0wn002/solo-suite-enchance.git
sites   https://git.chatgpt-team.site/2eb312a1-33c7-4816-91e5-007cfae402f8/appgprj_6a6776eb920881918c093b1e28e04127.git
```

The path segment `appgprj_6a6776eb920881918c093b1e28e04127` is **byte-identical
to `.openai/hosting.json`'s `project_id`**, and `git branch -r` lists a live
`sites/main`. The most likely deploy trigger is therefore **`git push sites
main`**, in the Heroku/Sites style where the hosting platform builds on receipt
of a push.

**Do not treat this as established.** A matching remote proves the repository
is *wired to* the Sites project; it does not prove that a push deploys, nor
what it deploys (branch mapping, build command, environment). Confirming it
empirically means performing a production deploy, which is not an acceptable
way to answer a documentation question.

**How this was missed**: the 2026-08-07 audit searched `package.json` for a
deploy script and `README.md` for deploy prose, found neither, and concluded
the trigger "is not fully established from the repository alone." It never ran
`git remote -v`. The repository did contain the answer — in git's own
configuration rather than in a tracked file.

**Smallest action that closes T25**: ask whoever owns the Sites project to
confirm (a) whether `git push sites main` is the deploy trigger, (b) which
branch production tracks, and (c) whether there is a rollback path at the Sites
layer distinct from `wrangler rollback`. Then replace this section with the
confirmed answer.

## Rollback plan

**Precondition**: identify the previous known-good deployment before you need
it — don't discover `wrangler deployments list` for the first time during an
incident.

1. **List recent deployments**:
   ```powershell
   npx.cmd wrangler deployments list
   ```
   This shows deployment IDs and timestamps for the Worker tied to this
   project's Cloudflare account.

2. **Roll back to the last known-good deployment**:
   ```powershell
   npx.cmd wrangler rollback [deployment-id]
   ```
   Omitting `[deployment-id]` rolls back to the immediately preceding
   deployment. Wrangler will prompt for confirmation — confirm only after
   verifying the target ID is actually the last good one, not just the most
   recent.

3. **If the regression is in the OpenAI "Sites" hosting layer rather than the
   underlying Worker** (unconfirmed mechanism, see above): there is currently
   no documented rollback path for that layer in this repository. Escalate to
   whoever manages the Sites project directly rather than assuming
   `wrangler rollback` covers it.

4. **Verify the rollback**: re-run the smoke assertions `tests/rendered-html.test.mjs`
   already encodes (200 response, correct `<title>`, no starter-template
   leftovers) against the live URL, not just locally — a rollback that
   restores old code but not old *config* (e.g. env vars) can still be broken.

5. **Record the incident**: append an entry to `.solo/decisions.md` (what
   broke, what was rolled back to, root cause once known) and update
   `.solo/risks.md` if the incident reveals a new risk class.

## Deploy history

Not yet tracked here. Once a real deploy trigger is confirmed (see above),
this section should log each production deploy: date, commit SHA, deployer,
and a one-line summary — so step 1 of the rollback plan becomes "check this
table" instead of "guess from `wrangler deployments list`."
