# Agent guidance

Project memory lives in `.solo/` — read `handoff.md` and `tasks.md` at session start; update them when work state changes.

Treat generated parity manifests, release metadata, checksums, and AgentRoom templates as code: regenerate them with their owning tool and run the focused regression tests before reporting success. Production-side-effect skills remain preview/plan-only until the user gives a distinct explicit confirmation through an authorized execution workflow.
