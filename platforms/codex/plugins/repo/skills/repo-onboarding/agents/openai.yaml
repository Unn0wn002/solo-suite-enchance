interface:
  display_name: "Repo Onboarding"
  short_description: "Run the Repo Onboarding Codex workflow"
  default_prompt: "Use $repo-onboarding to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false