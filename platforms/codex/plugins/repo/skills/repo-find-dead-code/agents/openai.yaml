interface:
  display_name: "Repo Find Dead Code"
  short_description: "Run the Repo Find Dead Code Codex workflow"
  default_prompt: "Use $repo-find-dead-code to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false