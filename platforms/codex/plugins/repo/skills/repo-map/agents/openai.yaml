interface:
  display_name: "Repo Map"
  short_description: "Run the Repo Map Codex workflow"
  default_prompt: "Use $repo-map to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false