interface:
  display_name: "Repo Analyzer"
  short_description: "Run the Repo Analyzer workflow in Codex"
  default_prompt: "Use $repo-analyzer to help with this project."
policy:
  allow_implicit_invocation: false