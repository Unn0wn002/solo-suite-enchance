interface:
  display_name: "Repo Dependency Map"
  short_description: "Run the Repo Dependency Map Codex workflow"
  default_prompt: "Use $repo-dependency-map to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false