interface:
  display_name: "Security Threat Model"
  short_description: "Run the Security Threat Model Codex workflow"
  default_prompt: "Use $security-threat-model to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false