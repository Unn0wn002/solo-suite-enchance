interface:
  display_name: "AI Compare Models"
  short_description: "Run the AI Compare Models Codex workflow"
  default_prompt: "Use $ai-compare-models to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false