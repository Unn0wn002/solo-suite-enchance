interface:
  display_name: "Growth Conversion Audit"
  short_description: "Run the Growth Conversion Audit Codex workflow"
  default_prompt: "Use $growth-conversion-audit to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false