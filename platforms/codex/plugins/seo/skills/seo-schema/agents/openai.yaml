interface:
  display_name: "SEO Schema"
  short_description: "Run the SEO Schema workflow in Codex"
  default_prompt: "Use $seo-schema to help with this project."
policy:
  allow_implicit_invocation: false
