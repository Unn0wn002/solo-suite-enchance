interface:
  display_name: "SEO Audit"
  short_description: "Run the SEO Audit Codex workflow"
  default_prompt: "Use $seo-audit to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false
