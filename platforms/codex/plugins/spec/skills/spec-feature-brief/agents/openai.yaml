interface:
  display_name: "Spec Feature Brief"
  short_description: "Run the Spec Feature Brief Codex workflow"
  default_prompt: "Use $spec-feature-brief to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false