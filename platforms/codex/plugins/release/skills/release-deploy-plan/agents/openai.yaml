interface:
  display_name: "Release Deploy Plan"
  short_description: "Run the Release Deploy Plan Codex workflow"
  default_prompt: "Use $release-deploy-plan to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false