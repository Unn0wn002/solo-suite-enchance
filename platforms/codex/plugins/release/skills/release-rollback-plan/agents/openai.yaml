interface:
  display_name: "Release Rollback Plan"
  short_description: "Run the Release Rollback Plan Codex workflow"
  default_prompt: "Use $release-rollback-plan to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false