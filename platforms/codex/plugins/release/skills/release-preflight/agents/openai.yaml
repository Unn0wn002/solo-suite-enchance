interface:
  display_name: "Release Preflight"
  short_description: "Run the Release Preflight Codex workflow"
  default_prompt: "Use $release-preflight to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false