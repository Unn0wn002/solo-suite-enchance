interface:
  display_name: "Browser Smoke Test"
  short_description: "Run the Browser Smoke Test Codex workflow"
  default_prompt: "Use $browser-smoke-test to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false