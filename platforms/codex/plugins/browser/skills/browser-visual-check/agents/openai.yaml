interface:
  display_name: "Browser Visual Check"
  short_description: "Run the Browser Visual Check Codex workflow"
  default_prompt: "Use $browser-visual-check to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false