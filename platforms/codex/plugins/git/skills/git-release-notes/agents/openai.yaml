interface:
  display_name: "Git Release Notes"
  short_description: "Run the Git Release Notes Codex workflow"
  default_prompt: "Use $git-release-notes to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false