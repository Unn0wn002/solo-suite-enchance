interface:
  display_name: "Git Create Branch"
  short_description: "Run the Git Create Branch Codex workflow"
  default_prompt: "Use $git-create-branch to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false