interface:
  display_name: "Git Commit Plan"
  short_description: "Run the Git Commit Plan Codex workflow"
  default_prompt: "Use $git-commit-plan to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false