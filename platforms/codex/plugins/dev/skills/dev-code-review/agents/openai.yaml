interface:
  display_name: "Dev Code Review"
  short_description: "Run the Dev Code Review Codex workflow"
  default_prompt: "Use $dev-code-review to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false