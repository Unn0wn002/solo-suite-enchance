interface:
  display_name: "Dev Fix Bug"
  short_description: "Run the Dev Fix Bug Codex workflow"
  default_prompt: "Use $dev-fix-bug to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false