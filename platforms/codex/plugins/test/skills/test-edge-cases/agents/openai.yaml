interface:
  display_name: "Test Edge Cases"
  short_description: "Run the Test Edge Cases Codex workflow"
  default_prompt: "Use $test-edge-cases to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false