interface:
  display_name: "Project PRD"
  short_description: "Run the Project PRD Codex workflow"
  default_prompt: "Use $project-prd to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false