interface:
  display_name: "Capability Routing"
  short_description: "Select a phase-owned Solo Suite capability set"
  default_prompt: "Use $capability-routing to select the smallest team for this request."
policy:
  allow_implicit_invocation: false
