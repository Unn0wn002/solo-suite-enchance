interface:
  display_name: "Project Architecture"
  short_description: "Run the Project Architecture Codex workflow"
  default_prompt: "Use $project-architecture to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false