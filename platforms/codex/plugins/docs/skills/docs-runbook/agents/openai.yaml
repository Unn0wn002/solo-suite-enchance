interface:
  display_name: "Docs Runbook"
  short_description: "Run the Docs Runbook Codex workflow"
  default_prompt: "Use $docs-runbook to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false