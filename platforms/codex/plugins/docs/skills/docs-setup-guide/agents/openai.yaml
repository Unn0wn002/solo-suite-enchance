interface:
  display_name: "Docs Setup Guide"
  short_description: "Run the Docs Setup Guide Codex workflow"
  default_prompt: "Use $docs-setup-guide to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false