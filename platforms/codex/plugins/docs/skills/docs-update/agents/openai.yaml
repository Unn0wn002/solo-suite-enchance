interface:
  display_name: "Docs Update"
  short_description: "Run the Docs Update Codex workflow"
  default_prompt: "Use $docs-update to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false