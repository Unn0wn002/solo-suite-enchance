interface:
  display_name: "Site Doctor SEO"
  short_description: "Run the Site Doctor SEO Codex workflow"
  default_prompt: "Use $site-doctor-seo to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false