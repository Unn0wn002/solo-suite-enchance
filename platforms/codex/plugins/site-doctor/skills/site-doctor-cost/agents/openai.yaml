interface:
  display_name: "Site Doctor Cost"
  short_description: "Run the Site Doctor Cost Codex workflow"
  default_prompt: "Use $site-doctor-cost to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false