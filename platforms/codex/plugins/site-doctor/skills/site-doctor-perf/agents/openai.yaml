interface:
  display_name: "Site Doctor Perf"
  short_description: "Run the Site Doctor Perf Codex workflow"
  default_prompt: "Use $site-doctor-perf to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false