interface:
  display_name: "Site Doctor Audit API"
  short_description: "Run the Site Doctor Audit API Codex workflow"
  default_prompt: "Use $site-doctor-audit-api to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false