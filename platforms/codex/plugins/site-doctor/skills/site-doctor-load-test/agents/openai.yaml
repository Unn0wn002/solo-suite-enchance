interface:
  display_name: "Site Doctor Load Test"
  short_description: "Run the Site Doctor Load Test Codex workflow"
  default_prompt: "Use $site-doctor-load-test to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false