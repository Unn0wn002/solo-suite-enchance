interface:
  display_name: "Database Fix"
  short_description: "Run the Database Fix workflow in Codex"
  default_prompt: "Use $database-fix to help with this project."
policy:
  allow_implicit_invocation: false