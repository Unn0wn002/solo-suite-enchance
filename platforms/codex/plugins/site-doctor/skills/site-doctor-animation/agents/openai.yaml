interface:
  display_name: "Site Doctor Animation"
  short_description: "Run the Site Doctor Animation Codex workflow"
  default_prompt: "Use $site-doctor-animation to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false
