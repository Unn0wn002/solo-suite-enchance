interface:
  display_name: "Site Doctor Audit DB"
  short_description: "Run the Site Doctor Audit DB Codex workflow"
  default_prompt: "Use $site-doctor-audit-db to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false