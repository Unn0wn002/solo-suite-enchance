interface:
  display_name: "Site Doctor Backups"
  short_description: "Run the Site Doctor Backups Codex workflow"
  default_prompt: "Use $site-doctor-backups to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false