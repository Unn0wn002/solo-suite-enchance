interface:
  display_name: "Site Doctor Audit Deps"
  short_description: "Run the Site Doctor Audit Deps Codex workflow"
  default_prompt: "Use $site-doctor-audit-deps to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false