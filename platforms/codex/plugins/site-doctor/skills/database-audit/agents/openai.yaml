interface:
  display_name: "Database Audit"
  short_description: "Run the Database Audit workflow in Codex"
  default_prompt: "Use $database-audit to help with this project."
policy:
  allow_implicit_invocation: false