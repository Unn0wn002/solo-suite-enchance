interface:
  display_name: "Solo Sync Grafana"
  short_description: "Run the Solo Sync Grafana Codex workflow"
  default_prompt: "Use $solo-sync-grafana to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false
