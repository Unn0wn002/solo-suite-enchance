interface:
  display_name: "Solo Start Session"
  short_description: "Run the Solo Start Session Codex workflow"
  default_prompt: "Use $solo-start-session to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false
