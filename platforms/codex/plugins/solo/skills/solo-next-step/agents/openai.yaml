interface:
  display_name: "Solo Next Step"
  short_description: "Run the Solo Next Step Codex workflow"
  default_prompt: "Use $solo-next-step to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false
