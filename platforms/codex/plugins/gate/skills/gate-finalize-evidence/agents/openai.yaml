interface:
  display_name: "Gate Finalize Evidence"
  short_description: "Run the Gate Finalize Evidence Codex workflow"
  default_prompt: "Use $gate-finalize-evidence to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false