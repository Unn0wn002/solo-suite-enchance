interface:
  display_name: "Stack Audit Tags"
  short_description: "Run the Stack Audit Tags Codex workflow"
  default_prompt: "Use $stack-audit-tags to run this workflow with the provided context."
policy:
  allow_implicit_invocation: false