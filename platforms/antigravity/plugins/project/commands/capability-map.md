---
description: Select the smallest phase-owned capability set
argument-hint: [request, project phase, or risk]
---
Use the capability-routing skill for: $ARGUMENTS

Read `capability-inventory.json`, `CAPABILITY_ROADMAP.md`, and the relevant
`.solo/` memory files. Write `.solo/capability-plan.md` with the activated
plugins, skills, commands, inputs, outputs, handoffs, evidence gates, excluded
capabilities, and the next exact command.

Do not activate every plugin by default. Route Graphify, bounded native search,
primary official documentation, and Repomix. Rejected audit sources have no
activation path.

## Output

End with: **Owner -> Inputs -> Work -> Output -> Evidence -> Next command**.
