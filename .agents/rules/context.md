# Portable context rule

Start with metadata and the active phase profile. Load only the selected skill body, then only the references needed for the current decision. Keep generated graphs, repository dumps, dependencies, build output, media, and coverage outside startup context.
