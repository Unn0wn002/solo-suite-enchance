# Role handoff workflow

1. Name the active role and profile.
2. Record inputs and assumptions.
3. Produce the role's declared outputs.
4. Run the profile verification commands.
5. Report pass, fail, `BLOCKED`, or `NOT_EXECUTED` with evidence.
6. Hand off only the bounded artifacts needed by the next role.
