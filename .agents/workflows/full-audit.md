# Full extension audit workflow

Use the `full-audit` profile and the `agent-extension-audit` skill. Keep external checkouts under `.tmp/agent-extension-audit/`, never execute their code, continue past individual failures, and update reports, manifest, lock, validation evidence, and Graphify only after reviewed repository changes are complete.
